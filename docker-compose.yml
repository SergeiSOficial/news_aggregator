version: '3.9'

services:
    microservice:
        build:
            context: ./microservice
        image: news-maker-kerry
        container_name: news-maker-kerry
        restart: unless-stopped

        command: ["python3", "main.py"]
        # tty: true
        # stdin_open: true