version: '3.9'

services:
    microservice:
        build:
            context: ./microservice
        image: news-maker
        container_name: news-maker
        restart: unless-stopped

        command: ["python3", "main.py"]
        # tty: true
        # stdin_open: true